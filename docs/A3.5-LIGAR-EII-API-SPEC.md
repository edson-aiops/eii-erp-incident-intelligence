# A3.5 — Ligar eii_api.py ao Deep Agents

> **Objetivo:** Consolidar Deep Agents como pipeline único de produção.
> Remover glm_router (pipeline leve). Integrar scrubber v2 na entrada.

**Status:** Spec (não implementação)
**Versão:** 1.0
**Data:** 2026-09-03

---

## 1. Contexto

### 1.1 Estado atual

`eii_api.py` (FastAPI) roda dois pipelines em paralelo dentro de `run_analysis()`:

```python
def run_analysis(job_id: str, prompt: str):
    qwen_result = qwen_local(prompt)       # Qwen 14B (Ollama local)
    glm_result = glm_remote(prompt)        # GLM-5.3 (OpenRouter)
    jobs[job_id] = {"qwen": qwen_result, "glm": glm_result}
```

Não usa Deep Agents, não usa scrubber, não usa SmartRouter. É o pipeline leve original.

### 1.2 Decisão

**Matar glm_router.py completamente.** Deep Agents é agora o único motor:

- ✅ Integrado com scrubber v2 (A23)
- ✅ Usa SmartRouter com fail-closed (A3)
- ✅ Recuperação via ChromaDB
- ✅ Grading e qualidade de diagnóstico

---

## 2. Mudança de fluxo

### 2.1 De (hoje)

```
POST /api/analyze
  └─ run_analysis(job_id, prompt)
      ├─ qwen_local(prompt)
      └─ glm_remote(prompt)
      → jobs[job_id] = {qwen: ..., glm: ...}
```

### 2.2 Para (A3.5)

```
POST /api/analyze
  └─ run_analysis_deep_agents(job_id, xml, incident_id)
      ├─ PIIScrubber.scrub(xml)
      ├─ diagnose_incident_deep_agents(scrubbed_xml, incident_id)
      └─ jobs[job_id] = {diagnosis: ..., routing_decision: ..., is_safe_for_remote: ...}
```

---

## 3. Contrato público — alterações

### 3.1 eii_api.py — imports

**Remover:**
```python
from glm_router import qwen_local, glm_remote
```

**Adicionar:**
```python
import asyncio
from src.privacy.scrubber import PIIScrubber
from src.deep_agents_wrapper import diagnose_incident_deep_agents
```

### 3.2 eii_api.py — função run_analysis()

**Mudar assinatura:**

Antes (síncrono, recebe prompt genérico):
```python
def run_analysis(job_id: str, prompt: str):
```

Depois (assíncrono, recebe XML + contexto):
```python
async def run_analysis_deep_agents(
    job_id: str,
    xml: str,
    incident_id: str,
    mentor_mode: bool = False
):
```

**Implementação:**

```python
async def run_analysis_deep_agents(
    job_id: str,
    xml: str,
    incident_id: str,
    mentor_mode: bool = False
):
    """
    Executa Deep Agents com scrubber obrigatório.

    Fail-closed: se scrubber levanta exceção, retorna erro (não tenta cloud).
    """
    try:
        # 1. Scrubber obrigatório
        scrubber = PIIScrubber()
        try:
            scrub_result = scrubber.scrub(xml, event_type="S-2200")  # detectar tipo depois
            is_safe = scrub_result.is_safe_for_remote
            scrubbed_xml = scrub_result.scrubbed_payload
            token_map = scrub_result.token_map
        except Exception as e:
            # Fail-closed: PII não seguro, não tenta diagnóstico
            with jobs_lock:
                jobs[job_id] = {
                    "status": "error",
                    "error": f"Scrubber exception: {e}",
                    "is_safe_for_remote": False,
                }
            return

        # 2. Chamar Deep Agents
        result = await diagnose_incident_deep_agents(
            xml=scrubbed_xml,  # Payload scrubbed
            incident_id=incident_id,
            mentor_mode=mentor_mode,
            force_local=not is_safe,  # Se PII não seguro, força local
        )

        # 3. Restaurar tokens na resposta
        if token_map and result.get("diagnostico"):
            restaurado = scrubber.restore(result["diagnostico"], token_map)
            result["diagnostico"] = restaurado

        # 4. Salvar resultado
        with jobs_lock:
            jobs[job_id] = {
                "status": "completed",
                "diagnosis": result,
                "is_safe_for_remote": is_safe,
            }
    except Exception as e:
        with jobs_lock:
            jobs[job_id] = {"status": "error", "error": str(e)}
```

### 3.3 Rota /api/analyze — mudança

A rota HTTP não muda de assinatura, mas precisa extrair XML e incident_id do corpo.

**Modelo Pydantic novo:**

```python
class DiagnoseRequest(BaseModel):
    xml: str
    incident_id: str
    mentor_mode: bool = False
```

**Rota:**

```python
@app.post("/api/analyze")
async def analyze(background_tasks: BackgroundTasks, body: DiagnoseRequest):
    """Analisa XML do eSocial via Deep Agents."""
    try:
        if not body.xml:
            raise HTTPException(status_code=400, detail="XML obrigatorio")

        job_id = str(uuid.uuid4())
        with jobs_lock:
            jobs[job_id] = {"status": "processing"}

        # Agendar tarefa assíncrona
        background_tasks.add_task(
            run_analysis_deep_agents,
            job_id,
            body.xml,
            body.incident_id,
            body.mentor_mode,
        )
        return {"job_id": job_id, "status": "processing"}
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

### 3.4 Rota /api/analyze-file — mudança

Ler XML do arquivo, depois chamar Deep Agents (não glm_router).

```python
@app.post("/api/analyze-file")
async def analyze_file(background_tasks: BackgroundTasks, body: FileRequest):
    """Analisa arquivo XML via Deep Agents."""
    try:
        if not os.path.isfile(body.filepath):
            raise HTTPException(status_code=404, detail=f"Arquivo nao encontrado: {body.filepath}")

        with open(body.filepath, 'r', encoding='utf-8') as f:
            xml_content = f.read()

        # Extrair incident_id do XML (ou gerar UUID)
        incident_id = str(uuid.uuid4())

        job_id = str(uuid.uuid4())
        with jobs_lock:
            jobs[job_id] = {"status": "processing"}

        background_tasks.add_task(
            run_analysis_deep_agents,
            job_id,
            xml_content,
            incident_id,
            mentor_mode=False,
        )
        return {"job_id": job_id, "status": "processing", "filepath": body.filepath}
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

### 3.5 Rota /api/results/{job_id} — sem mudança

Continua lendo `jobs[job_id]`, mas agora com estrutura diferente:

```python
@app.get("/api/results/{job_id}")
async def get_result(job_id: str):
    """Retorna resultado do job."""
    with jobs_lock:
        if job_id not in jobs:
            raise HTTPException(status_code=404, detail="Job nao encontrado")
        return jobs[job_id]
```

---

## 4. Invariantes

1. **Scrubber é mandatório.** Nenhuma chamada a Deep Agents sem scrubber antes.
2. **Fail-closed:** se scrubber falha, retorna erro (status 500), nunca tenta cloud.
3. **Tokens são restaurados** na resposta antes de retornar ao cliente.
4. **token_map não sai.** Fica local (em memória).
5. **glm_router.py é removido** completamente — nenhuma importação deixada.

---

## 5. Testes black-box esperados

### 5.1 Integração Deep Agents via POST /api/analyze

**Cenário:** Enviar XML com PII via REST.

```python
def test_analyze_chama_deep_agents_nao_glm_router():
    """POST /api/analyze chama Deep Agents com scrubber."""
    xml_com_cpf = "<evento><cpfTrab>11111111111</cpfTrab>..."

    response = client.post(
        "/api/analyze",
        json={"xml": xml_com_cpf, "incident_id": "evt123"}
    )

    assert response.status_code == 202
    job_id = response.json()["job_id"]

    # Aguardar processamento
    import time
    time.sleep(2)

    result = client.get(f"/api/results/{job_id}").json()

    assert result["status"] == "completed"
    assert "diagnosis" in result  # Deep Agents retorna diagnosis
    assert "CPF_" in result["diagnosis"] or "11111111111" not in result["diagnosis"]
```

### 5.2 Fail-closed em scrubber

**Cenário:** XML malformado.

```python
def test_scrubber_exception_retorna_erro():
    """Se scrubber falha, retorna erro 500 (não tenta cloud)."""
    xml_invalido = "<evento>nao eh xml"

    response = client.post(
        "/api/analyze",
        json={"xml": xml_invalido, "incident_id": "evt123"}
    )

    assert response.status_code == 202
    job_id = response.json()["job_id"]

    import time
    time.sleep(2)

    result = client.get(f"/api/results/{job_id}").json()

    assert result["status"] == "error"
    assert "is_safe_for_remote" in result or result["error"]
```

### 5.3 Arquivo via POST /api/analyze-file

**Cenário:** Analisar arquivo XML local.

```python
def test_analyze_file_chama_deep_agents():
    """POST /api/analyze-file lê arquivo e chama Deep Agents."""
    # Criar arquivo temp
    import tempfile
    with tempfile.NamedTemporaryFile(mode='w', suffix='.xml', delete=False) as f:
        f.write("<evento><cpfTrab>11111111111</cpfTrab>...")
        filepath = f.name

    try:
        response = client.post(
            "/api/analyze-file",
            json={"filepath": filepath, "question": "Analisar"}
        )

        assert response.status_code == 202
        result = response.json()
        assert "job_id" in result
    finally:
        os.unlink(filepath)
```

### 5.4 Tokens são restaurados

**Cenário:** Resposta contém dados reais, não tokens.

```python
def test_tokens_restaurados_na_resposta():
    """Diagnóstico retorna ao usuário com dados reais."""
    xml = "<evento><cpfTrab>11111111111</cpfTrab><nmTrab>MARIA</nmTrab>..."

    response = client.post(
        "/api/analyze",
        json={"xml": xml, "incident_id": "evt123"}
    )

    job_id = response.json()["job_id"]

    import time
    time.sleep(2)

    result = client.get(f"/api/results/{job_id}").json()
    diagnosis = result["diagnosis"]

    # Se o diagnóstico menciona o trabalhador, deveria ser o nome real
    if "MARIA" in diagnosis or "Maria" in diagnosis:
        assert "11111111111" in diagnosis or "CPF_" not in diagnosis
```

---

## 6. EVIDENCE_PACK esperado

Quando implementado:

```markdown
## A3.5 — Ligar eii_api.py ao Deep Agents

### Diff
- eii_api.py: remover glm_router, adicionar deep_agents_wrapper + scrubber
- Testes: 4 cenários

### Testes
- test_analyze_chama_deep_agents_nao_glm_router: ✅
- test_scrubber_exception_retorna_erro: ✅
- test_analyze_file_chama_deep_agents: ✅
- test_tokens_restaurados_na_resposta: ✅

### Blast radius
- Localizado: eii_api.py
- Remove: glm_router.py (pode ser deletado)
- Não toca: Deep Agents, SmartRouter, scrubber (já testados)

### Rollback
git revert <commit A3.5>

### Métrica
Pipeline único: 100% das requisições via Deep Agents (A23 + A3).
```

---

## 7. Limitações e notas

1. **event_type não é detectado automaticamente.** Hardcoded como "S-2200" no
   scrubber. Implementação futura: parser de XML para extrair tipo.

2. **jobs em memória.** Se server reinicia, histórico se perde. Usar DB em produção.

3. **mentor_mode e force_local são opcionais** na requisição, mas passados ao Deep Agents.
   Se não especificados, Deep Agents usa defaults.

---

## 8. Próximos passos (fora desta spec)

- **A12:** ADR sobre pseudonimização
- **A24:** Testes de integração e2e com payload real
- **A25:** Ligar Gradio UI ao novo eii_api.py (se houver)
