# Dependencias minimas para rodar a suite de testes no CI.
# Mapeadas por analise de imports — a suite nao usa torch, chromadb
# nem sentence-transformers; instala em segundos.
pytest>=8.0
pytest-asyncio>=0.23
openai>=1.0.0
anthropic>=0.30
pydantic>=2.0.0
langchain-core>=0.1.0
tenacity>=8.2.0
requests>=2.31.0
python-dotenv>=1.0.0
